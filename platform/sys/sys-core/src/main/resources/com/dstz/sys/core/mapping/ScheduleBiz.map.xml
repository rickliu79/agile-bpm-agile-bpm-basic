<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dstz.sys.core.dao.ScheduleBizDao">
	<resultMap id="ScheduleBiz" type="com.dstz.sys.core.model.ScheduleBiz">
		<id property="id" column="id" jdbcType="VARCHAR"/>
		<result property="scheduleId" column="schedule_id" jdbcType="VARCHAR"/>
		<result property="bizId" column="biz_id_" jdbcType="VARCHAR"/>
		<result property="source" column="source" jdbcType="VARCHAR"/>
	</resultMap>
	
	<insert id="create" parameterType="com.dstz.sys.core.model.ScheduleBiz">
		INSERT INTO c_schedule_biz
		(id,schedule_id,biz_id,source)
		VALUES 
		(#{id,jdbcType=VARCHAR}, #{scheduleId,jdbcType=VARCHAR}, #{bizId,jdbcType=VARCHAR}, #{source,jdbcType=VARCHAR})
	</insert>
	
	<select id="get"   parameterType="java.lang.String" resultMap="ScheduleBiz">
		SELECT * FROM c_schedule_biz 
		WHERE 
		id=#{id}
	</select>
	
	<select id="query" parameterType="java.util.Map" resultMap="ScheduleBiz">
		SELECT * FROM c_schedule_biz
		<where>
			<if test="whereSql!=null">
				${whereSql}
			</if>
		</where>
		<if test="orderBySql!=null">
			ORDER BY ${orderBySql}
		</if>
		<if test="orderBySql==null">
			ORDER BY id DESC
		</if>
	</select>
	
	<update id="update" parameterType="com.dstz.sys.core.model.ScheduleBiz">
		UPDATE c_schedule_biz SET
		schedule_id=#{scheduleId,jdbcType=VARCHAR},
		biz_id=#{bizId,jdbcType=VARCHAR},
		source=#{source,jdbcType=VARCHAR}
		WHERE
		id=#{id}
	</update>
	
	<delete id="remove" parameterType="java.lang.String">
		DELETE FROM c_schedule_biz 
		WHERE
		id=#{id}
	</delete>

	
</mapper>